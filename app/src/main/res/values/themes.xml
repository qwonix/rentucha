<resources>
    <!-- Base application theme. -->
    <style name="Theme.Rentucha" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="MaterialCardView.Corners" parent="@style/Widget.MaterialComponents.CardView">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.MaterialCardView.Cut
        </item>
    </style>

    <style name="ShapeAppearanceOverlay.MaterialCardView.Cut" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">30dp</item>
        <item name="cornerSizeTopRight">30dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <style name="BottomNavigationView" parent="@style/TextAppearance.AppCompat.Caption">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="BottomNavigationView.Active" parent="@style/TextAppearance.AppCompat.Caption">
        <item name="android:textSize">11sp</item>
    </style>

    <style name="MaterialCardView.SearchSettings" parent="@style/Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">25dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <style name="MaterialCardView.SearchSettings.Selected">
        <item name="cardCornerRadius">30dp</item>
        <item name="cardElevation">8dp</item>
    </style>

    <style name="MaterialCardView.Cleared" parent="@style/Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">0dp</item>
        <item name="cardElevation">0dp</item>
        <item name="strokeColor">@color/transparent</item>
        <item name="strokeWidth">0dp</item>
    </style>

    <style name="MaterialCardView.ImageIconButton" parent="@style/Widget.MaterialComponents.CardView">
        <item name="android:layout_gravity">end|center_vertical</item>
        <item name="cardCornerRadius">360dp</item>
        <item name="cardElevation">4dp</item>
        <item name="strokeColor">@color/gray_300</item>

    </style>
</resources>